{
  "name": "goodagent-desktop",
  "version": "1.0.1",
  "description": "GoodAgent Desktop — Electron GUI wrapper",
  "author": "quanzefeng <quanzefeng@gmail.com>",
  "homepage": "https://github.com/quanzefeng/GoodAgent",
  "type": "module",
  "main": "main.mjs",
  "scripts": {
    "start": "cross-env ELECTRON_DISABLE_SANDBOX=1 electron .",
    "dev": "electron . --dev",
    "dist:win": "electron-builder --win nsis",
    "dist:mac": "electron-builder --mac dmg",
    "dist:linux": "electron-builder --linux deb AppImage",
    "dist:all": "electron-builder --win nsis --mac dmg --linux deb AppImage",
    "lint": "eslint *.mjs core/*.mjs",
    "lint:fix": "eslint --fix *.mjs core/*.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "download-model": "node scripts/download-model.mjs",
    "postinstall": "node scripts/download-model.mjs"
  },
  "publish": {
    "provider": "github",
    "owner": "quanzefeng",
    "repo": "GoodAgent"
  },
  "build": {
    "appId": "com.goodagent.desktop",
    "productName": "GoodAgent",
    "asar": true,
    "files": [
      "*.mjs",
      "preload.cjs",
      "preload.js",
      "renderer/**",
      "core/**"
    ],
    "extraResources": [
      {
        "from": "../dist",
        "to": "cli-dist"
      },
      {
        "from": "models",
        "to": "models"
      }
    ],
    "icon": "icon.ico",
    "win": {
      "target": "nsis",
      "icon": "icon.ico",
      "artifactName": "GoodAgent-${version}-Windows-${arch}.${ext}",
      "requestedExecutionLevel": "asInvoker"
    },
    "mac": {
      "target": "dmg",
      "icon": "icon.icns",
      "artifactName": "GoodAgent-${version}-macOS-${arch}.${ext}",
      "category": "public.app-category.developer-tools"
    },
    "linux": {
      "target": [
        "deb",
        "AppImage"
      ],
      "icon": "icon.png",
      "artifactName": "GoodAgent-${version}-Linux-${arch}.${ext}",
      "category": "Development"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "directories": {
      "output": "release"
    }
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "cross-env": "^10.1.0",
    "electron": "^40.8.0",
    "electron-builder": "^26.0.0",
    "eslint": "^10.4.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "electron-updater": "^6.8.3",
    "qrcode": "^1.5.4"
  }
}
